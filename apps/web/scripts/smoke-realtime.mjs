#!/usr/bin/env node
/**
 * Ручной чеклист проверки realtime (SSE) и чатов.
 * Запуск: npm run smoke:realtime
 */
console.log(`
=== Smoke: realtime / чаты (ручная проверка) ===

1) Клиент: два окна браузера (или инкогнито + обычное).
   - Войти как клиент → Сообщения: бейдж и список обновляются без F5.
   - Открыть чат по заказу → статус «Соединение: онлайн», сообщения с аватаром и именем.

2) Мастер: /tech → чат по треду.
   - Сообщения клиента и мастера с разными аватарками, сверху блок «Клиент / Вы».

3) Связка: из панели мастера отправить сообщение в тред, привязанный к repairId.
   - У клиента в inbox/чате появляется сервисное сообщение с именем мастера.

4) Разлогин / истёкшая сессия:
   - При истечении stream-токена ожидается статус про сессию (auth_error), без бесконечного спама.

5) Главная / Отслеживание (после входа):
   - Карточки тянутся с GET /api/v1/client/repairs, без локального mockRepairOrders.

Готово. Для автоматизации позже можно добавить e2e (Playwright).
`);
