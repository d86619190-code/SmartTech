# Сервис фронта (Vite → preview). Версия Node задаётся в nixpacks.toml (Vite 8 → Node ≥20.19).
# Переменные `VITE_*` — в Railway → Variables сервиса.
[build]
builder = "nixpacks"
buildCommand = "bun install && bun run build"

[deploy]
startCommand = "bun run start"
